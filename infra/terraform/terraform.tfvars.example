aws_region         = "us-east-1"
project_name       = "kevops-mcp"
creator            = "ganesh"
purpose            = "kd-ai-league"
instance_type      = "t3a.micro"
root_volume_size_gb = 12
# Optional pin if you want deterministic AMI and no surprise replacement
# ami_id = "ami-xxxxxxxx"

# Optional if you want SSH
# key_name = "your-ec2-keypair-name"
ssh_cidr_blocks = ["0.0.0.0/0"]

# Push this repo to GitHub (or another git host) and set URL + ref.
repo_url = "https://github.com/<your-user>/<your-repo>.git" # REQUIRED
repo_ref = "main"

# Keep 80 for easier access in demos.
host_port = 80

# Optional custom domain. If empty, the server uses <public-ip>.nip.io.
app_domain = ""

auth_required = true
auth0_issuer  = "https://YOUR_TENANT.us.auth0.com/" # REQUIRED
auth0_audience = "https://kev-ops-mcp-api/mcp" # REQUIRED (stable Auth0 API Identifier; do not bind to EC2/IP)
auth0_audience_aliases_csv = "" # Optional, e.g. "http://old-host.nip.io/mcp"
auth0_resource = "https://kev-ops-mcp-api/mcp" # Optional; defaults to auth0_audience
auth0_jwks_uri = "https://YOUR_TENANT.us.auth0.com/.well-known/jwks.json" # REQUIRED

auth0_tier_claim   = "https://kevops.example.com/tier"
auth0_roles_claim  = "https://kevops.example.com/roles"
auth0_default_tier = "free"
auth_required_scopes_csv = "mcp:tools"

# Optional
nvd_api_key = ""
log_level   = "info"

# Optional OSV scan tuning
osv_query_timeout_ms = 20000
osv_query_batch_timeout_ms = 30000
osv_batch_chunk_size = 40
osv_batch_concurrency = 4
osv_batch_fallback_concurrency = 6
osv_max_pages_per_query = 4
